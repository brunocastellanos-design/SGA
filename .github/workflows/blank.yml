name: Ejecutar Script y Subir Excel 🚀

on:
  workflow_dispatch: # Permite dispararlo manualmente o vía API

jobs:
  run_script:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Configurar Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.x'

      - name: Instalar dependencias
        run: |
          python -m pip install --upgrade pip
          # Añade 'openpyxl' para manejo de Excel moderno (.xlsx)
          pip install pandas xlrd openpyxl unidecode 

      - name: Ejecutar script Python
        # Asegúrate de que este script crea el archivo Excel
        run: python HISTORICO.py 

      # AÑADIDO: Paso para subir el Excel como Artefacto
      - name: Subir Excel como Artefacto
        uses: actions/upload-artifact@v4
        with:
          name: ExcelGeneradoPorScript # Nombre del artefacto (lo usaremos en Power Automate)
          path: datos_generados.xlsx    # <<< IMPORTANTE: REEMPLAZA ESTE NOMBRE 
                                        # con el nombre real del archivo que crea tu script Python
          retention-days: 1             # El archivo se conservará por 1 día
